[project]
name = "beacon"
version = "0.1.0"
description = "An everyday workflow orchestrator"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "pytest>=9.0.3",
]
